

default_app_config = "properties.apps.PropertiesConfig"
# This ensures that the PropertiesConfig class is used to configure the app
# and that the signals are properly registered when the app is ready.